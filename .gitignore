/node_modules
/.next
/out
/build
.DS_Store
*.pem
npm-debug.log*
.env*.local
next-env.d.ts
.vercel
.env*
# but DO track the documented example (no secrets)
!.env.example

# private proof/source docs — never commit, never serve, never upload
_private-source/

# local build info
tsconfig.tsbuildinfo
.claude/
